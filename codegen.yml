schema: src/graphql/schema.graphql
generates:
  src/graphql/graphql.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      contextType: ../types#Context
      mappers:
        Property: ../types#PrismaProperty
        WeatherData: ../types#PrismaWeatherData
      scalars:
        DateTime: Date
